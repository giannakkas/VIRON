# ═══════════════════════════════════════════════════
# VIRON AI BRAIN — Configuration
# ═══════════════════════════════════════════════════
# Copy to .env and fill in your API keys.
# At least ONE cloud provider is needed for full functionality.

# ─── Local LLM (Ollama) ──────────────────────────
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=phi3

# ─── Cloud AI Providers ──────────────────────────
# Claude — Best for: reasoning, literature, history, explanations
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-opus-4-20250514

# Gemini — Best for: Greek language, translation, geography, multilingual
GOOGLE_API_KEY=AI...
GEMINI_MODEL=gemini-2.0-flash

# ChatGPT — Best for: math, science, coding, step-by-step solutions
OPENAI_API_KEY=sk-...
CHATGPT_MODEL=gpt-4o-mini

# ─── Routing Strategy ────────────────────────────
# best_one  = ⚡ Turbo  — #1 ranked AI only         (~$3-5/mo)
# race      = 🏎️ Race   — Top 2, use fastest         (~$5-8/mo)
# verify    = ✅ Check  — Answer + verify with 2nd   (~$6-10/mo)
# consensus = 🧠 Smart  — All 3, merge best answer   (~$15-25/mo)
#
# Student can also switch by voice: "turbo mode", "smart mode" etc.
ROUTING_STRATEGY=best_one

# ─── Subject Routing (auto-detected) ─────────────
# Math/Science/Code     → ChatGPT first
# Greek/Translation     → Gemini first
# Literature/History    → Claude first
# Creative Writing      → Claude first
# Greetings/Simple      → Ollama (local, free)

# ─── Timeouts & Retries ──────────────────────────
CLOUD_TIMEOUT=60
LOCAL_TIMEOUT=30
MAX_RETRIES=3

# ─── Confidence Gate ─────────────────────────────
# If Ollama response seems uncertain, auto-escalate to cloud
CONFIDENCE_GATE=true
